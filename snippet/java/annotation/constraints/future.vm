#*
  Produces a empty string or @Future
  and adds the library org.hibernate.validator.Past to the current class.
  This snippet expects to be called in the context of a pattern attribute
*#
##stop if $currentModelAttribute is null
#if(!$currentModelAttribute)
  $generator.error("currentModelAttribute not found in context")
#end
#set($attribute=$currentModelAttribute)
#set($future="")## set to empty string explicitly, otherwise value is not set to null if nothing found
#set($future=$generator.getElementProperty(${attribute},"validator.date.future"))
#if($future == "true")
  #if($attribute.getAttributeValue("type")!="Date")
    $generator.error("Invalid validator.date.future property found at attribute $attribute.getAttributeValue('name')")
  #end
  $generator.addLibraryToClass("javax.validation.constraints.Future")##
  @Future
#end
##

